export const PRODUCTS_PER_PAGE = 12;
export const BASE_URL = 'https://dummyjson.com';
export const refs = {
  categoriesList: document.querySelector('.categories'),
  if (refs.someElement) {
  refs.someElement.textContent = '...';
}
};
